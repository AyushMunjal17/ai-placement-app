# Database Configuration
# Use MongoDB Atlas or local MongoDB
MONGO_URI=mongodb://localhost:27017/ai-placement
# OR MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-placement

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this

# Server Configuration
PORT=5000
NODE_ENV=development

# AI Configuration (Google Gemini API - Free tier)
# Get your free API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Note: Code execution uses Piston API (no API key needed, unlimited submissions!)
